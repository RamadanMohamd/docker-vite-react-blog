export interface NewsResposne {
  articles: Array<NewsArticleType>;
  status: string;
  totalResults: number;
}

export interface NewsArticleType {
  status: string;
  totalResults: number;
  articles: Array<{
    source: {
      id: string | null;
      name: string;
    };
    author: string | null;
    title: string;
    description: string | null;
    url: string;
    urlToImage: string | null;
    publishedAt: string;
    content: string | null;
  }>;
}
